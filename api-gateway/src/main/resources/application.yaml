server:
  port: 8080

spring:
  application:
    name: api-gateway

spring.cloud.gateway:
  routes:
    - id: product-service
      uri: http://localhost:8081
      predicates:
        - Path=/products/**